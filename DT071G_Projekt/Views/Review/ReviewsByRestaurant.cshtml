@model List<string> // Hämtar modellen för stränglistan som skrivs ut nedan till skärmen

@{
    ViewData["Title"] = "Reviews By Restaurant";
}

<h2>Choose a restaurant to view reviews:</h2>

@if (Model.Count == 0)
{
    <p>No restaurants available.</p>
}
else
{
    <ul>
        @for (int i = 0; i < Model.Count; i++) // Här loopas alla restauranger igenom och skrivs nedan ut till skärmen
        {
            <li>
                <a asp-controller="Review" asp-action="DisplayReviewsByRestaurant" asp-route-restaurant="@Model[i]">@Model[i]</a>
            </li>
        }
    </ul>
}

<p>
    <a asp-controller="Home" asp-action="Index" class="text-dark text-muted">Back to Home Page</a>
</p>
